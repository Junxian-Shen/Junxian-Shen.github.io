# 个人主页维护与发布

英文首页：https://junxian-shen.github.io/；中文首页：https://junxian-shen.github.io/zh/。

两个版本共用模板、论文列表和配色，不必维护两套网站。首屏 Research overview 已移除；新版采用浅色个人介绍横幅、图标栏目标题和独立的 News 时间线面板，消息增多后在面板内滚动。论文只展示标题、作者、简写发表信息和资料链接；报告采用紧凑列表。字体随网站提供，无需连接外部字体网站。日常内容文件和推送方式不变。

## 一、首次配置：只给这个仓库绑定专用账号

仓库属于 **Junxian-Shen**，不是常用的 **datawine** 账号。使用仓库专用 Deploy key，只授予这一个仓库的访问权限，不切换全局 Git / GitHub CLI 登录，不改 datawine 的密钥和配置。

1. 在项目目录准备专用密钥和项目级配置（只运行一次）：

   ```bash
   cd "/Users/datawine/Documents/Coding/个人主页/Junxian-Shen.github.io"
   bash scripts/configure-github.sh --prepare
   ```

   密钥存放在项目的 `.git/homepage-ssh/`，连接方式、提交姓名和邮箱只写入项目 `.git/config`。私钥无口令，由仅本人可读的文件权限保护，不会进入提交或生成的网站。不要把整个 `.git` 文件夹公开、共享或发给别人；换电脑建议重新创建并授权，不要复制私钥。

2. 在浏览器的独立个人资料或隐私窗口登录 **Junxian-Shen**，不必退出原来的 datawine 会话。打开 [这个仓库的 Deploy keys 设置](https://github.com/Junxian-Shen/Junxian-Shen.github.io/settings/keys)，点击 **Add deploy key**。标题可填 `Homepage on this Mac`，粘贴 `.git/homepage-ssh/id_ed25519.pub` 公钥的完整内容，勾选 **Allow write access**，再添加。只添加 `.pub` 公钥，不能添加或发送私钥。

3. 添加后回到项目目录验证：

   ```bash
   bash scripts/configure-github.sh
   bash scripts/publish.sh --check
   ```

准备步骤先固定专用密钥，因此在公钥未授权时只会连接失败，不会回退使用 datawine 的密钥。验证步骤仅接受目标仓库的 Deploy key 或 Junxian-Shen 的专用账号密钥；推送脚本也会再次核对。SSH 忽略全局连接配置、禁用共享密钥代理和代理转发，不向代理添加密钥；不修改已有主机指纹记录。这个项目关闭提交自动签名，避免借用 datawine 的全局签名身份。全局设置和其他项目保持原样。

如果已经有 Junxian-Shen 专用密钥，可把它的绝对路径传给 `configure-github.sh` 使用，但不要传 datawine 的密钥。项目内的专用密钥可在仓库 Deploy keys 页面撤销；撤销后其他账号和仓库不受影响。Deploy key 的授权范围见 [GitHub 官方说明](https://docs.github.com/en/authentication/connecting-to-github-with-ssh/managing-deploy-keys#deploy-keys)。

在 [仓库 Pages 设置](https://github.com/Junxian-Shen/Junxian-Shen.github.io/settings/pages) 确认发布来源。如果使用分支发布，设置为 **Deploy from a branch → master → /(root)**。这是 Jekyll 源码网站，不要上传 `_site/`。如果已经使用其他部署方式，请先确认，不要盲目更改。具体机制见 [GitHub 官方说明](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)。

## 二、以后每次怎么更新、推送

1. 修改内容文件，按下文填写。
2. 本地预览，检查中英文和资料链接。
3. 在项目目录执行：

   ```bash
   bash scripts/publish.sh "更新个人简介和 News"
   ```

这条命令会核对账号、仓库和分支，检查远端有无冲突，然后提交主页相关文件并推送。不会强制覆盖远端，也不会上传本地构建缓存。私钥有口令时，终端可能要求输入口令。

4. 到 [Actions](https://github.com/Junxian-Shen/Junxian-Shen.github.io/actions) 查看 Pages 构建成功，再打开线上中英文首页。推送成功不等于部署成功，以构建结果和实际网页为准。

发布包含下列主页数据、模板、样式、论文、报告，以及 `files/`、`images/` 内的内容更新。放进这些目录前，确认文件可以公开；不要放私钥、未公开稿件或私人文件。错误账号、已有暂存内容或远端冲突会使脚本停止。网络导致推送失败时，本地提交保留，可重新运行同一命令。

## 三、日常改哪里

| 想修改的内容 | 文件 |
| --- | --- |
| 中英文简介、职位、学院、研究方向、简历链接 | `_data/home.yml` |
| 邮箱、英文姓名、照片、英文单位、学术链接 | `_config.yml` 中的 `author` |
| 最新动态 News | `_data/news.yml` |
| 论文 | `_publications/`，每篇一个 Markdown 文件 |
| 科研项目、基金资助 | `_data/projects.yml` |
| 教学课程、开课学期 | `_data/teaching.yml` |
| 学生姓名、学位与年级 | `_data/students.yml` |
| 报告 | `_talks/`，每次一个 Markdown 文件 |
| 中英文导航、栏目标题、固定标签 | `_data/home_ui.yml` |
| 配色 | `_data/theme.yml` |
| PDF、简历、幻灯片 | `files/` |

`home.yml`、`news.yml`、`projects.yml`、`teaching.yml` 和 `students.yml` 的英文使用原字段，中文使用 `_zh` 后缀。没填中文时回退到英文，建议每次一起填写。论文标题、作者使用正式发表版本，中英文共用，不需要翻译或重复录入。

### 添加一条 News

在 `_data/news.yml` 最上面增加一条，替换示例内容：

```yaml
- date: "2026 · Sep"
  date_zh: "2026 · 09"
  category: "Paper"
  text: "English news text."
  text_zh: "对应的中文消息。"
  url: "/files/new-paper.pdf"
```

按文件顺序显示，最新的放最上面。类别可用 `Paper`（论文）、`Service`（学术服务）、`Award`（获奖）、`Teaching`（教学）。基金资助只维护在科研项目栏目，不加入 News 或 About。没有链接就不写 `url`；跳转到本页教学栏目可用 `"#teaching"`，不会切换语言。正文用普通文字，不需要 HTML。日期保留引号，只有年份可写 `"2026"`；未确认日期时省略 `date` 和 `date_zh`，不显示占位日期。课程学期可写 `"2026 · Fall"` / `"2026 · 秋"`。

### 添加论文

在 `_publications/` 新建文件，例如 `2026-new-paper.md`：

```yaml
---
title: "论文正式英文标题"
collection: publications
category: conferences
permalink: /publication/new-paper
date: 2026-09-07
venue: "会议或期刊名称"
venue_short: "会议或期刊简称及年份"
excerpt: "Junxian Shen, Author Two, Author Three"
paperurl: "/files/new-paper.pdf"
---
```

替换成真实信息，`permalink` 不能重复。`excerpt` 填作者列表，用英文逗号分隔；自己的名字自动加粗。会议用 `conferences`，期刊沿用 `manuscripts`。有幻灯片可加 `slidesurl: "/files/new-slides.pdf"`；没有 PDF 就不写 `paperurl`。

自动按日期倒序、按年份分组。`venue_short` 是可选的首页简写，例如 `ACM SIGCOMM 2023`；不填会显示完整的 `venue`。不需要摘要、配图或额外介绍。旧论文详情页的摘要可保留，不影响首页的简洁列表。站内资料链接统一写 `/files/文件名.pdf`，外部链接使用完整的 `https://...`。

如果正式发表信息只有年份，可像 Clover 一样使用 `date: 2026-01-01` 加 `date_precision: year`。这里的 1 月 1 日仅作排序键，不是发表日期；首页和论文列表只显示年份，详情页不输出精确到日的发表元数据。已知正式在线发表日时直接填真实日期，不加 `date_precision`。

### 添加或更新科研项目

编辑 `_data/projects.yml`，一条项目记录包含中英文题名和资助机构：

```yaml
- title: "Official English project title"
  title_zh: "项目正式中文名称"
  funder: "National Natural Science Foundation of China (NSFC)"
  funder_zh: "国家自然科学基金"
```

确认信息后可补 `period` / `period_zh`（起止时间）、`role` / `role_zh`（本人角色）、`grant_number`（批准号，保留引号）和 `funding` / `funding_zh`（资助经费）。未确认的字段不写，页面不会显示空标签；不希望展示经费时，删除两个 `funding` 字段即可。基金类别可补在资助名称中。当前项目已按资助通知补充批准号 `62602206`、执行期 2027 年 1 月至 2029 年 12 月、资助经费 30 万元；类别已由本人确认为青年科学基金项目（C 类），英文为 `NSFC Young Scientists Fund (Category C)`。本人角色暂不标注，项目尚未到执行期，不标为“在研”。

### 添加或更新教学工作

编辑 `_data/teaching.yml`，例如当前课程：

```yaml
- title: "Operating Systems"
  title_zh: "操作系统"
  term: "Fall 2026"
  term_zh: "2026 年秋季学期"
```

同一门课以后继续开设，可在 `term` / `term_zh` 中增加学期；新增课程就再加一条。课程网站上线后可加 `url`，没有网站时保持普通文字，不显示空链接。项目和教学栏目会自动生成，中英文导航同步更新；以后只需维护这些内容文件，不需要改页面模板。

### 添加或更新学生

编辑 `_data/students.yml`，例如当前学生：

```yaml
- name: "Yuzhuo Tan"
  name_zh: "谭宇焯"
  initials: "YT"
  stage: "Year 1 Master"
  stage_zh: "硕士一年级"
```

学生栏目位于教学工作之后，采用首字母圆形头像、姓名和学位/年级的小卡片，按文件顺序排列。在读人数自动统计；新增学生只需再加一条记录，不需要改页面模板。`initials` 填英文姓名首字母，不需要照片。每学年手动更新 `stage` / `stage_zh`；不要把年级当作自动递增值。当前只展示在读学生，不显示空的毕业生栏目。名单清空时请保留 `[]`，学生栏目及导航会自动隐藏。推送仍使用第二节的同一命令。

### 2026 年内容更新

已加入 TFCG、Clover 两篇论文及 `files/` 中对应 PDF；News 同步加入两篇论文和 2026 秋季《操作系统》教学，不显示基金资助消息。项目与课程的长期记录分别放在上述两个数据文件中。中英文 About 的研究段落已加入 IEEE ToN 和 Applied Soft Computing，不展示基金资助信息；教育经历段落、原有研究方向和报告内容未改。新论文信息按提供的 PDF 核对，Clover 的期刊名称、卷页和年份另按出版方登记的 [DOI 信息](https://doi.org/10.1109/TON.2026.3680829) 核对。

后续更新仍先检查本地预览，再按第二节的原有发布方式操作；专用 GitHub 身份仅用于本项目。是否已上线，以 GitHub Pages 部署结果和实际网页为准。

### 添加报告

在 `_talks/` 新建文件：

```yaml
---
title: "中文报告标题"
title_en: "English talk title"
collection: talks
type: "Talk"
permalink: /talks/new-talk
venue: "中文报告场合"
venue_en: "English venue"
date: 2026-09-07
slidesurl: "/files/new-talk.pdf"
---
```

有视频可加 `videourl`，值填实际视频链接。没有幻灯片或视频时不写对应字段。自动按日期排序。

### 调整配色

直接改 `_data/theme.yml` 的十六进制色号，两种语言一起生效。当前采用常用颜色表里的 Snow `#F9F7F3`、深蓝 `#3D5A80`、浅灰蓝 `#F1F2F6`，暖色、绿色作少量点缀。重新构建后预览即可。

## 四、本地预览

这台 Mac 的预览依赖已放在项目的 `local/bundle/`，不用依赖临时目录，也不会上传到 GitHub。启动预览：

```bash
bash scripts/preview.sh
```

然后打开 `http://127.0.0.1:4000/` 和 `http://127.0.0.1:4000/zh/`。

已有预览占用 4000 端口时，不要重复启动。脚本启动的新预览会监听内容修改；当前展示的预览没有自动监听，修改后可以重新构建：

```bash
bash scripts/preview.sh --build
```

修改 `_config.yml` 后，自动监听的预览也需要重启。`_site/` 是生成结果，不要编辑或上传。日常无需更换框架、安装前端工具或维护数据库。这份说明和 `scripts/` 不会作为网页发布。

换电脑时，需要先安装 Ruby / Bundler，再用 `bundle install --path local/bundle` 安装依赖；不要把这台 Mac 的本地依赖直接搬到不同系统。
